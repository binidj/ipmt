#pragma once

// class LZ77 
// {
// private:
//     static const size_t bufferSize = 1024 * 1024 * 1024;
//     static unsigned char buffer[bufferSize];
//     static const size_t searchBufferSize = 2048;
//     static const size_t lookAheadSize = 16;
// public:
//     static void Compress(FILE *inputFile, FILE *outputFile);
//     static void Decompress(FILE *inputFile, FILE *outputFile);
// };